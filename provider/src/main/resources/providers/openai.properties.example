# OpenAI 提供商配置示例
# 将此文件复制为 openai.properties 并修改为你的实际配置

# 基础配置
provider=openai
model.name=gpt-3.5-turbo
api.key=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
api.endpoint=https://api.openai.com/v1/chat/completions

# 高级配置
timeout=30000
max.retries=3
retry.delay=1000

# 模型特定配置
model.temperature=0.7
model.max.tokens=2048
model.top.p=1.0
model.frequency.penalty=0.0
model.presence.penalty=0.0

# 代理配置（如需通过代理访问OpenAI API）
#proxy.host=proxy.example.com
#proxy.port=8080
#proxy.username=username
#proxy.password=password

# 组织ID（如果您属于多个组织）
#organization.id=org-xxxxxxxxxxxxxxxxxxxxx 